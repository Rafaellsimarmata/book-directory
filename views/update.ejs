<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Directory</title>

    <!-- bootstrap css -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous" />

    <!-- Bootstrap Font Icon CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" />
  </head>
  <body>
    <div class="container" style="margin-top: 50px">
      <h1 class="text-center">Add Book</h1>

      <% for(data of data) {%>
      <form  action="/updatebook/<%= data.id %>" method="post">
        <div class="container col-6" style="margin-top: 50px">
          <div class="form-floating mb-3">
            <input type="JSON" class="form-control" id="name" name="name" placeholder="name@example.com" value="<%= data.name %>" />
            <label for="name">Name</label>
          </div>
          <div class="form-floating mb-3">
            <input type="JSON" class="form-control" id="nib" name="nib" placeholder="nib" value="<%= data.nib %>" />
            <label for="nib">NIB</label>
          </div>
          <div class="form-floating mb-3">
            <input type="JSON" class="form-control" id="genre" name="genre" placeholder="genre" value="<%= data.genre %>"/>
            <label for="genre">Genre</label>
          </div>
          <div class="form-floating mb-3">
            <input type="JSON" class="form-control" id="author" name="author" placeholder="author" value="<%= data.author %>"/>
            <label for="author">Author</label>
          </div>
          <% } %>
          <div class="container" style="margin-top: 25px">
            <div class="d-grid gap-2 col-4 mx-auto">
              <input type='button' class="btn btn-primary" name="submit-button" value="add-book" data-bs-toggle="modal" data-bs-target="#exampleModal"></input>
            </div>
          </div>
              <!-- Modal -->
          <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog">
              <div class="modal-content">
                <div class="modal-header">
                  <h1 class="modal-title fs-5" id="exampleModalLabel">Apakah data sudah benar ? </h1>
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                  <button type="submit" class="btn btn-primary">Add</button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </form>
    </div>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  </body>
</html>
